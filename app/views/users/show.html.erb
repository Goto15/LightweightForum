<h1><%= @user.username %></h1>
<div class='topics'>Topics:</div>
<ul>
    <div class='topics'>
    <% @topics.each do |topic| %>
        <li>
            <%= link_to topic.name, topic_path(topic) %>
        </li>
    <% end %>
    </div>
</ul>